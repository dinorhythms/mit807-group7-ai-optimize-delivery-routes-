services:
  - type: web
    name: metroswift
    runtime: python312
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn web_app:application
    envVars:
      - key: PYTHON_VERSION
        value: 3.12
      - key: PORT
        value: 5000
    healthCheckPath: /health
